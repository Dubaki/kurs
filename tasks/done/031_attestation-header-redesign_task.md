📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Редизайн шапки (Header) на странице Аттестации (attestation.html). Мы переносим основные навигационные кнопки в шапку, делаем их аккуратными и заменяем текстовый логотип на изображение.

ЗАВИСИМОСТИ: 028_attestation-isolation_task
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: attestation.html
ТИП: ui
</context>

<task>
1. В левой части хедера `attestation.html` убрать текстовый логотип и вставить изображение: `<img src="photos/start.png" alt="VibeCraft" class="h-8 w-auto">` (настроить высоту по дизайну).
2. Удалить все старые/огромные кнопки перехода из основной Hero-секции страницы (оставить только информационный контент).
3. В центр/правую часть хедера добавить аккуратные навигационные кнопки (с одинаковым стилем, например, `text-sm font-bold uppercase tracking-wider hover:text-primary transition-colors`):
   - "Пример отчёта Аттестации 66ai" (ведет на `otchet.html`)
   - "Рассчитать стоимость" (ведет на якорь формы или модалку лидов)
   - "Тарифы" (ведет на якорь с тарифами)
   - "Цены" (ведет на `price.html`)
4. ВЕРИФИКАЦИЯ: Открыть `attestation.html`. Логотип-картинка отображается слева. 4 аккуратные кнопки меню отображаются в хедере. Клик по "Цены" ведет на (пока пустую) страницу price.html.
5. Заполнить COMPLETION LOG и перенести в done/.
</task>

<rules>
- ИСПОЛНИТЕЛЬ: Claude Code.
- СТИЛЬ КНОПОК В ХЕДЕРЕ: Они не должны быть громоздкими (не как CTA-кнопки), а выглядеть как стилизованное навигационное меню.
</rules>