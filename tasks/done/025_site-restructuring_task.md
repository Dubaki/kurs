📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Глобальное изменение структуры сайта для перехода на архитектуру «Сплит-Шлюза».
Интерактивный шлюз (Index1.html) становится главной страницей (index.html), а текущая страница курса (index.html) переезжает на новый адрес (vibe.html). Необходимо обеспечить целостность всех внутренних ссылок.

ЗАВИСИМОСТИ: 024_gateway-final-polish_task (done)
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: index.html, index1.html, about.html, cases.html, blog.html, attestation.html
ТИП: infra | refactor
</context>

<task>
1. Переименовать текущий файл `index.html` в `vibe.html`.
2. Переименовать текущий файл `index1.html` в `index.html` (теперь это главная).
3. Массовое обновление ссылок во ВСЕХ файлах проекта (index.html, vibe.html, cases.html и др.):
   - Все ссылки, ведущие на "главную" (логотип, пункт НАЧАЛО), должны указывать на новый `index.html`.
   - На новой главной (бывший шлюз) ссылка на карточке «Обучение Вайбкодингу» должна вести на `vibe.html`.
   - В навигационных меню (header/footer) на всех страницах пункт «ПРОГРАММА» или «ОБУЧЕНИЕ» должен вести на `vibe.html`.
4. Проверить навигацию в мобильном меню и в нижнем закрепленном меню (Mobile Fixed Bottom Nav) — они должны соответствовать новой структуре.
5. ВЕРИФИКАЦИЯ:
   - Открыть `index.html` (шлюз) -> нажать на карточку обучения -> должен открыться `vibe.html`.
   - С любой страницы (например, `cases.html`) нажать на логотип -> должен открыться шлюз (`index.html`).
6. Заполнить COMPLETION LOG и перенести в done/.
</task>

<rules>
- ИСПОЛНИТЕЛЬ: Qwen Code (задача на структурную целостность).
- ПРОВЕРКА: Использовать `grep` или поиск по проекту, чтобы не пропустить захардкоженные ссылки `index.html`.
- ВАЖНО: Не удалять старые файлы до завершения переименования, чтобы не потерять код.
</rules>

---

## COMPLETION LOG
**Статус:** completed
**Дата завершения:** 2026-04-13
**Исполнитель:** Qwen Code

### Сделано
- `index.html` → `vibe.html` (страница курса)
- `Index1.html` → `index.html` (шлюз теперь главная)
- Шлюз: карточка «Обучение Вайбкодингу» → `vibe.html`
- `vibe.html`: якоря стали локальными (`#process`, `#program`, `#faq`)
- `about.html`, `cases.html`, `blog.html`, `attestation.html`: ПРОЦЕСС → `vibe.html#process`, ПРОГРАММА → `vibe.html#program`, FAQ → `vibe.html#faq`
- Логотип и НАЧАЛО на всех страницах → `index.html` (шлюз)

### Изменённые файлы
- `index.html` (бывший Index1.html) — карточка обучения → `vibe.html`
- `vibe.html` (бывший index.html) — якоря локальные
- `about.html`, `cases.html`, `blog.html`, `attestation.html` — ссылки на курс → `vibe.html`

### Верификация
- [x] Шлюз (index.html) → карточка обучения → vibe.html
- [x] Логотип с любой страницы → index.html (шлюз)
- [x] НАЧАЛО → index.html на всех страницах
- [x] ПРОЦЕСС/ПРОГРАММА/FAQ → vibe.html#... на внешних страницах
- [x] vibe.html якоря локальные (#process, #program, #faq)
- [x] Мобильное меню обновлено

### Побочные эффекты / риски
- Старые закладки на index.html теперь ведут на шлюз, а не на курс — ожидаемое поведение.

### Открытые вопросы
- Нужно ли добавить редирект для внешних ссылок, которые вели на старый index.html#program?
