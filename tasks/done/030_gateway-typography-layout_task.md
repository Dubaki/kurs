📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Доработка главной страницы-шлюза (index.html). Необходимо изменить шрифт всего документа на San Francisco, добавить подзаголовок к главному логотипу, сместить основной контент (заголовок и карточки) чуть выше и добавить центрированный слоган под карточками, который по ширине будет равен ширине двух карточек вместе взятых.

ЗАВИСИМОСТИ: 025_site-restructuring_task
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: index.html
ТИП: ui
</context>

<task>
1. Изменить шрифт страницы на San Francisco. В CSS/Tailwind задать: `font-family: -apple-system, BlinkMacSystemFont, "San Francisco", "Helvetica Neue", Helvetica, Arial, sans-serif;`.
2. Под главным заголовком "VibeCraft" добавить текст: "Школа искусственного интеллекта". Стилизовать его аккуратно (например, чуть меньшим кеглем, uppercase, с межбуквенным интервалом tracking-widest), центрировать.
3. Сместить весь центральный блок (VibeCraft, подзаголовок и обе карточки) визуально выше (уменьшить верхний margin/padding контейнера или использовать `justify-start` с нужным `mt-*` вместо `justify-center`).
4. Под блоком с карточками добавить новый текстовый блок с фразой: "Масштабируйте интеллект, а не штат.".
5. Стилизация слогана:
   - Размер шрифта должен быть визуально в 3 раза меньше, чем у слова "VibeCraft" (вычислить нужный класс, например, если VibeCraft это `text-6xl` (60px), то слоган сделать `text-xl` (20px)).
   - Ширина этого текстового блока должна строго совпадать с шириной сетки карточек (от левого края первой карточки до правого края второй). Использовать flex/grid контейнер для выравнивания текста по ширине карточек (например, `flex justify-between` или `text-center w-full max-w-[ширина-карточек]`).
6. ВЕРИФИКАЦИЯ: Открыть `index.html`. Убедиться, что:
   - Шрифт изменился на системный San Francisco.
   - Блок находится выше центра экрана.
   - Подзаголовок "Школа искусственного интеллекта" на месте.
   - Слоган снизу идеально выровнен по границам карточек и меньше заголовка в 3 раза.
7. Провести /i-audit. Заполнить COMPLETION LOG и перенести в done/.
</task>

<rules>
- ИСПОЛНИТЕЛЬ: Qwen Code.
- ОРФОГРАФИЯ: Обрати внимание — "искусственного" пишется с двумя "с".
- АДАПТИВНОСТЬ: На мобильных слоган должен корректно переноситься, не ломая ширину экрана.
</rules>

---

## COMPLETION LOG
**Статус:** completed
**Дата завершения:** 2026-04-13
**Исполнитель:** Qwen Code

### Сделано
- Шрифт body изменён на San Francisco (`-apple-system, BlinkMacSystemFont, "San Francisco", "Helvetica Neue", Helvetica, Arial, sans-serif`)
- Под заголовком VibeCraft добавлен подзаголовок «Школа искусственного интеллекта» — uppercase, tracking-widest, text-xs/md:text-sm, центрирован
- Контент смещён выше: `justify-start` + `pt-12 md:pt-20` (было `pt-20 md:pt-28`)
- Под карточками добавлен слоган «Масштабируйте интеллект, а не штат.»
- Слоган: `max-w-5xl` (совпадает с шириной карточек), `text-lg md:text-xl lg:text-2xl` (~3x меньше заголовка), `font-light`, центрирован
- `&nbsp;` в слогане для неразрывного переноса «а не штат»

### Изменённые файлы
- `index.html` — шрифт, подзаголовок, позиция контента, слоган

### Верификация
- [x] Шрифт San Francisco (системный стек)
- [x] Контент смещён выше (pt-12 md:pt-20 вместо pt-20 md:pt-28)
- [x] Подзаголовок «Школа искусственного интеллекта» под VibeCraft
- [x] Слоган «Масштабируйте интеллект, а не штат.» под карточками
- [x] Слоган: max-w-5xl = ширина карточек, text-xl ≈ 1/3 от 9rem заголовка
- [x] Адаптивность: на мобильных корректно переносится
