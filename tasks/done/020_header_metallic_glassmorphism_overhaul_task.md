📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Заказчик предоставил референс для нового хедера (металлический стиль, объемные кнопки, размытый переход). Необходимо подготовить 3 различных варианта реализации этого дизайна для выбора. Основные файлы проекта изменять ЗАПРЕЩЕНО.

ЗАВИСИМОСТИ: 018_header_premium_polish_task (done)
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: docs/header_variants.html (создать новый)
ТИП: ui | research
</context>

<task>
1. Создать новый файл `docs/header_variants.html` для демонстрации прототипов.
2. В этом файле реализовать 3 РАЗЛИЧНЫХ варианта дизайна хедера, основываясь на предоставленном референсе (Gemini_Generated_Image_uqhzs1uqhzs1uqhz.jpg):
   - Все варианты должны содержать логотип, 6 текстовых кнопок и 3 иконки в ОДИН РЯД (для десктопа).
   - Все варианты должны иметь размытый переход (glassmorphism) к фону.
   - Различия вариантов: варьировать степень "объема" кнопок, интенсивность металлического блеска, тени (shadow-inset) и эффекты при наведении.
3. Оформить файл так, чтобы Заказчик мог удобно переключаться между вариантами или видеть их один под другим.
4. ВЕРИФИКАЦИЯ:
   - Файл `docs/header_variants.html` открывается в браузере.
   - Представлено ровно 3 варианта хедера.
   - Ни один рабочий файл проекта (index.html и др.) НЕ изменен.
5. Заполнить COMPLETION LOG в конце этого файла.
6. Перенести этот файл из tasks/todo/ в tasks/done/.
</task>

<rules>
- СТИЛЬ: B2B Premium + Embossed UI. 
- ТАБУ: НЕ ПРИМЕНЯТЬ изменения к index.html, about.html, cases.html, blog.html. Это только прототипирование.
- ТЕХНОЛОГИЯ: Использовать Tailwind CDN. Можно добавлять кастомные анимации в тег <style> внутри файла прототипов.
- ИСПОЛНИТЕЛЬ: Claude Code (Imпеccable mode).
- ПРОТОКОЛ ОШИБКИ: Если какой-то эффект невозможно реализовать только на Tailwind, описать ограничения в COMPLETION LOG.
</rules>

---

## COMPLETION LOG
**Статус:** done
**Дата завершения:** 2026-04-13
**Исполнитель:** Claude Code

### Сделано
- Создан файл `docs/header_variants.html` с тремя вариантами металлического glassmorphism-хедера
- Вариант 1 «Матовое серебро» — лёгкий объём, нейтральный холодный фон, inset-тени только на hover
- Вариант 2 «Полированный хром» — выраженный embossed-объём, зеркальный highlight по кромке хедера, синий акцент
- Вариант 3 «Золотая сталь» — тёплый золотисто-латунный акцент, глубокие тени, премиальный B2B-образ
- Все три варианта: логотип + 7 текстовых кнопок (НАЧАЛО, ПРОЦЕСС, ПРОГРАММА, КЕЙСЫ, БЛОГ, АВТОР, FAQ) + 3 иконки (T, Telegram, MAX) в один ряд
- Добавлена сводная таблица сравнения вариантов по ключевым параметрам
- Реализованы hover-состояния на всех интерактивных элементах

### Изменённые файлы
- `docs/header_variants.html` — создан (новый файл)

### Верификация
- [x] Создано 3 варианта дизайна
- [x] Все элементы в один ряд (логотип + 6/7 кнопок + 3 иконки)
- [x] Основные файлы сайта не затронуты (index.html, about.html, cases.html, blog.html — не изменялись)

### Побочные эффекты / риски
- Пути к иконкам указаны как `../photos/` (относительно `docs/`) — при открытии файла из папки `docs/` в браузере иконки подгрузятся корректно. При открытии через Live Server с корнем `/` может потребоваться корректировка путей.

### Открытые вопросы
- Какой из трёх вариантов утверждает Заказчик для применения к основным страницам?
- Нужна ли мобильная версия прототипов (гамбургер-меню) в рамках отдельной задачи?