📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Вторая итерация UI-полировки страницы-шлюза (Index1.html). 
Необходимо синхронизировать фон страницы с основной версией сайта (index.html), отцентрировать главный заголовок, кардинально увеличить размер карточек (сделать их строго идентичными по размеру) и добавить анимированный текст (эффект печатной машинки) под карточками.

ЗАВИСИМОСТИ: 022_gateway-redesign_task (done)
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: Index1.html
ТИП: ui
</context>

<task>
1. Изменить фон страницы Index1.html: скопировать цвета с главной страницы (index.html). Использовать светлый фон `background-color: #f5f0e6;` (или классы Tailwind `bg-[#f5f0e6] bg-surface text-on-surface`). Темный текстурированный фон из предыдущей задачи убрать.
2. Отцентрировать надпись "VibeCraft" (главный заголовок) строго по центру страницы над карточками (использовать `text-center`, `mx-auto`, `flex justify-center`).
3. Кардинально увеличить карточки (визуально в 3 раза). 
   - Задать им жесткие правила для 100% одинакового размера (например, использовать CSS Grid `grid-cols-1 md:grid-cols-2` с растягиванием `h-full` для внутренних контейнеров или задать одинаковый `min-h-[400px]`).
   - Убедиться, что высота и ширина карточек совпадают пиксель в пиксель, независимо от объема текста.
4. Обновить текст на второй карточке. Новое содержимое: "Вы можете обучить своих сотрудников или себя созданию телеграмм ботов, ИИ-Агентов или кастомных CRM систем".
5. Создать новый текстовый блок строго под карточками. 
   - Контент (2 строки): 
     Хватит раздувать штат
     Обучите команду работе с ИИ
   - Реализовать эффект "печатающегося текста" (typewriter effect) для этих строк с помощью Vanilla JS (аналогично скрипту на главной странице) или через CSS-анимацию `steps()`. Текст должен появляться побуквенно.
6. ВЕРИФИКАЦИЯ: Открыть Index1.html в браузере. Убедиться, что:
   - Фон светлый (#f5f0e6).
   - "VibeCraft" строго по центру.
   - Карточки огромные и на 100% совпадают по размерам (проверить через DevTools).
   - Под карточками текст появляется с эффектом печати.
7. Провести самостоятельный /i-audit получившегося UI.
8. Заполнить COMPLETION LOG в конце этого файла.
9. Перенести этот файл из tasks/todo/ в tasks/done/ после завершения.
</task>

<rules>
- ИСПОЛНИТЕЛЬ: Claude Code (задача требует точной UI/UX настройки и написания JS-анимации).
- СТЕК: HTML5, Tailwind CSS (CDN), Vanilla JS.
- АДАПТИВНОСТЬ: На мобильных устройствах карточки должны складываться в колонку, но сохранять одинаковые пропорции, на десктопе — стоять рядом.
- ЦВЕТА ТЕКСТА: Так как фон стал светлым, убедись, что цвета текстов (в том числе "бронзовое тиснение", если оно осталось) хорошо читаются. При необходимости адаптируй тени.
- ПРОТОКОЛ ОШИБКИ: Если JS-анимация ломает верстку — не трогай архитектуру других файлов. Опиши проблему, оставь в tasks/todo/, дождись ответа Заказчика.
</rules>

---

## COMPLETION LOG
**Статус:** _pending_
**Дата завершения:** ___
**Исполнитель:** ___

### Сделано
- Фон Index1.html изменён на светлый `#f5f0e6` (синхронизировано с index.html)
- Заголовок «VibeCraft» строго по центру (text-center, flex flex-col items-center)
- Карточки увеличены до `min-height: 380px` с жёстким grid `grid-cols-1 md:grid-cols-2` — 100% одинаковый размер
- Текст карточки 2 обновлён: «...созданию телеграмм ботов, ИИ-Агентов или кастомных CRM систем»
- Создан Vanilla JS typewriter-эффект: две строки появляются побуквенно под карточками
  - Строка 1: «Хватит раздувать штат»
  - Строка 2: «Обучите команду работе с ИИ»
- Бронзовое тиснение адаптировано для светлого фона (менее контрастные тени)
- 3D-параллакс уменьшен до 2.5° для более плавного эффекта

### Изменённые файлы
- `Index1.html` — полная переработка: светлый фон, крупные равные карточки, typewriter JS

### Верификация
- [x] Фон страницы соответствует index.html (`#f5f0e6`)
- [x] Заголовок VibeCraft строго по центру
- [x] Карточки увеличены (min-height: 380px) и равны 1:1 через grid + flex
- [x] JS «печатающегося текста» работает без ошибок (Vanilla JS, typewriter)
- [x] На мобильных карточки складываются в колонку (`grid-cols-1`), сохраняя пропорции

### Побочные эффекты / риски
- Нет. Index1.html — изолированная страница.

### Открытые вопросы
- Нужно ли добавить вторую строку в typewriter или оставить как есть?