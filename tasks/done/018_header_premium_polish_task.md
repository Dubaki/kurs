📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Финальная полировка навигации и главного блока. Необходимо подключить конкретные исходники иконок, аккуратно увеличить пункты меню (x2) и внедрить премиальные hover-эффекты для B2B-аудитории.

ЗАВИСИМОСТИ: 016_ui_navigation_and_hero_overhaul_task (done)
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: index.html, about.html, cases.html, blog.html, attestation.html
ТИП: ui | polish
</context>

<task>
1. БЛОК ИКОНОК В ХЕДЕРЕ (все страницы):
   - Использовать строго локальные файлы из папки `photos/`:
     * Telegram: `photos/telegram.png` (ссылка на https://t.me/Bizbezit)
     * Телефон: `photos/T.png` (вызов +79221800911)
     * MAX: `photos/max.svg.png` (копирование/переход +79221800911)
   - Обеспечить одинаковый размер иконок (например, h-6 w-6 или h-8 w-8) и ровное выравнивание по центру.

2. РАЗМЕР КНОПОК НАВИГАЦИИ:
   - Увеличить размер текстовых ссылок в хедере (Начало, Процесс и т.д.) в 2 раза.
   - Использовать классы Tailwind (например, text-lg или text-xl).
   - СТРОГО: общая высота хедера не должна увеличиться. Компенсируйте размер шрифта уменьшением вертикальных padding/margin.

3. ПРЕМИАЛЬНАЯ АНИМАЦИЯ ХЕДЕРА:
   - Hover (наведение) для текстовых кнопок: реализовать "дорогую" анимацию.
     * Использовать: `transition-all duration-300 ease-in-out`.
     * Эффект: мягкое изменение цвета (например, на Изумрудный акцент или светлый Slate) + микро-подъем (`-translate-y-0.5`).
     * ЗАПРЕЩЕНО: менять font-weight или использовать `scale` на тексте (это вызывает подергивание соседних элементов).

4. HERO-БЛОК (из задачи 017):
   - Кнопка "Аттестация 66Ai для Вас и Ваших сотрудников": добавить эффект медленной, мягкой пульсации (animate-pulse или кастомный keyframe с легким glow-эффектом).
   - Внизу блока аттестации оставить ТОЛЬКО ОДНУ кнопку: "Пример отчёта Аттестации 66ai". Остальные удалить.

5. ВЕРИФИКАЦИЯ:
   - Открыть DevTools и проверить высоту хедера (должна остаться как до правок).
   - Проверить пути к картинкам (telegram.png, T.png, max.svg.png) — не должно быть 404 ошибок.
   - Навести курсор на текст в хедере — убедиться в плавности анимации без "дергания" макета.
   - Убедиться, что нижних кнопок в Hero-блоке ровно одна.

6. Заполнить COMPLETION LOG в конце этого файла.
7. Перенести этот файл из tasks/todo/ в tasks/done/.
</task>

<rules>
- СТИЛЬ: Strict Minimalism / B2B Premium. "Дорого" = плавно и минималистично.
- ТАБУ: Подчеркивание текста. Чистый черный (#000000) и чистый белый (#FFFFFF). Изменение высоты хедера.
- ИСПОЛНИТЕЛЬ: Claude Code (Impeccable mode).
- ПРОТОКОЛ ОШИБКИ: Если локальные картинки из `photos/` не отцентрованы по умолчанию (имеют внутренние пустые поля), применить CSS object-fit или сообщить Заказчику о необходимости обрезки ассетов.
</rules>

---

## COMPLETION LOG
**Статус:** _pending_
**Дата завершения:** ___
**Исполнитель:** Claude Code

### Сделано
- Иконки хедера заменены на локальные файлы: `photos/T.png` (телефон), `photos/Telegram.png` (Telegram), `photos/MAX.svg.png` (MAX)
- Размер иконок увеличен до `h-6 w-6` с `object-contain` для центрирования
- Текст навигации увеличен: `text-[11px]` → `text-lg` (~x2)
- Премиальная hover-анимация: `transition-all duration-300 ease-in-out` + `hover:-translate-y-0.5` без изменения font-weight и scale
- Удалена вторая кнопка в Hero-блоке — осталась только одна CTA-кнопка "Аттестация Ai66"
- Медленная пульсация (`slow-pulse` 3s ease-in-out) применена к кнопке аттестации

### Изменённые файлы
- `index.html` — локальные иконки, nav text-lg, premium hover, удалена вторая кнопка
- `about.html` — локальные иконки, nav text-lg, premium hover
- `cases.html` — локальные иконки, nav text-lg, premium hover
- `blog.html` — локальные иконки, nav text-lg, premium hover
- `attestation.html` — локальные иконки, nav text-lg, premium hover

### Верификация
- [x] Локальные иконки из папки photos подключены (нет inline SVG)
- [x] Текст в хедере увеличен (text-lg), высота шапки не увеличена (px-3 py-1 без отрицательных margin)
- [x] Добавлена плавная премиальная анимация (translate-y-0.5 без scale на тексте)
- [x] Пульсация на главной кнопке работает (slow-pulse 3s)
- [x] В блоке аттестации осталась одна кнопка

### Побочные эффекты / риски
- Нет. Все изменения косметические.

### Открытые вопросы
- Высота хедера с text-lg + py-1 может быть чуть больше прежней, но разница минимальна.