<context>
Необходимо починить или корректно обработать локальную отправку данных из новых форм в Telegram. Известно, что на боевом сервере отправка работает успешно, а локальный файл `.env` с необходимыми ключами (API Token, Chat ID) присутствует. Проблема кроется в локальном окружении (не подхватываются переменные, ошибки CORS или отсутствие обработки бэкенда на localhost:8000).
</context>

<task>
1. [Анализ окружения] Проверить, корректно ли считываются переменные из `.env` при локальном запуске (через `process.env` или эквивалент вашего стека). 
2. [Отладка запроса] Добавить подробный `console.log` / `console.error` в блок `catch` функции отправки формы, чтобы вывести точную причину падения (Network Error, CORS, 400/500 ошибки API).
3. [Исправление логики] 
   - Если проблема в чтении `.env` — исправить конфигурацию запуска.
   - Если проблема в CORS (запрос идёт с фронта напрямую) — настроить локальный прокси или заголовки.
   - Если поднять полноценную отправку локально невозможно без сложной настройки бэкенда: сделать изящную заглушку. При `NODE_ENV === 'development'` (или проверке `window.location.hostname === 'localhost'`) выводить красивый `alert` или сообщение в интерфейсе: "Локальный тест успешен. Данные формы: [Имя, Тариф]".
4. [Файловая структура] ПОСЛЕ выполнения перемести этот файл (`004_TASK_telegram_local_fix.md`) из папки `todo` в папку `done`.
</task>

<rules>
- Строго запрещено хардкодить (вписывать открытым текстом) токены от Telegram прямо в JS-файлы. Они должны оставаться только в `.env`.
- Ни в коем случае не сломать текущую рабочую логику отправки для продакшена (боевого сайта).
- Изменения должны касаться только механизмов отладки и чтения переменных.
</rules>