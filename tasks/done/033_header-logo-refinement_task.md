📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Глобальная унификация шапки (Header) на всех страницах (кроме шлюза, если он изолирован). 
Необходимо оставить единый логотип `start.png`, отцентрировать кнопки, убрать кнопку "Тарифы" и добавить сложную анимацию (дыхание + эффект выдавленной кнопки) для логотипа.

ЗАВИСИМОСТИ: 031_attestation-header-redesign_task (done)
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: attestation.html, price.html, otchet.html
ТИП: ui
</context>

<task>
1. Во всех указанных файлах найти `<header>`:
   - Убедиться, что в качестве логотипа используется ТОЛЬКО изображение `<img src="photos/start.png" ...>`. Все текстовые элементы логотипа удалить.
2. Центрирование меню: 
   - Контейнер с кнопками в хедере должен располагаться по центру (например, через `flex-1 flex justify-center gap-6` или аналогичные Tailwind-классы).
3. Удалить кнопку "Тарифы" из навигации во всех файлах.
4. Анимация логотипа (start.png) на странице `attestation.html` (и связанных):
   - Добавить классы (или кастомный CSS) для легкого скругления: `rounded-xl` или `rounded-2xl`.
   - Добавить CSS-анимацию "дыхания" (медленный scale 1 -> 1.05 -> 1).
   - Сделать эффект "выдавленной кнопки" (neumorphism / embossed). Для этого в `<style>` добавить класс:
     `.embossed-logo { box-shadow: 4px 4px 10px rgba(0,0,0,0.1), -4px -4px 10px rgba(255,255,255,0.8); }` (адаптировать под текущий цвет фона `bg-[#f5f0e6]`).
5. ВЕРИФИКАЦИЯ: 
   - Открыть `attestation.html`.
   - В шапке слева — дышащий, чуть скругленный объемный логотип `start.png`.
   - В центре шапки — меню без кнопки "Тарифы".
6. Заполнить COMPLETION LOG.
7. Перенести файл в tasks/done/.
</task>

<rules>
- ИСПОЛНИТЕЛЬ: Claude Code.
- ЭСТЕТИКА: Эффект выдавленной кнопки должен быть очень мягким, без грубых черных теней. Использовать цвета, близкие к фону.
- НЕ ТРОГАТЬ логику отправки форм.
</rules>

---

## COMPLETION LOG
**Статус:** _pending_
**Дата завершения:** ___
**Исполнитель:** ___

### Сделано
- ___

### Изменённые файлы
- ___

### Верификация
- [ ] Кнопка "Тарифы" удалена везде
- [ ] Логотип start.png анимирован и выглядит объемным
- [ ] Меню отцентрировано