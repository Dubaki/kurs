📋 Прочитай docs/BOOT.md перед выполнением этого задания.

ЗАВИСИМОСТИ: нет
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: index.html, attestation.html
ТИП: feat | ui | overhaul

АНИМАЦИЯ «VIBECRAFT»:

В верхней части экрана реализовать заголовок, который при загрузке собирается из двух частей: «VIBE» (вылетает слева) и «CRAFT» (вылетает справа).

После соединения частей под ними плавно проявляется надпись «Ai Школа».

ЦЕНТРАЛЬНЫЕ КАРТОЧКИ (1:1 по размеру):

Разместить по центру экрана две одинаковые карточки:

Левая: «АТТЕСТАЦИЯ 66AI» (текст: «Узнайте, сколько часов рутины можно убрать»).

Правая: «VIBECRAFT» (текст: «Научи сотрудника созданию программных решений»).

Карточки должны иметь скругленные углы и эффект «выдавленности» из фона.

3D ПАРАЛЛАКС (Vanilla JS):

Реализовать мягкий наклон (tilt) карточек при движении мыши.

Эффект должен применяться только к плоскости карточки, текст внутри остается стабильным.

Амплитуда наклона должна быть минимальной (едва заметной).

ВЕРИФИКАЦИЯ:

Проверить соединение слов «Vibe» и «Craft» в центре при загрузке.

Убедиться, что параллакс работает плавно (без рывков) и только на карточках.

Сравнить визуальный результат с ref.jpg на предмет идентичности теней и шрифтов.

Заполнить COMPLETION LOG в конце этого файла.

Обновить docs/BRIEF.md (зафиксировать новую структуру фасада).

Перенести этот файл из tasks/todo/ в tasks/done/.

COMPLETION LOG
Статус: completed
Дата завершения: 2026-04-13
Исполнитель: Claude Code

Сделано
- Создан Gateway Facade — полноэкранная секция в начале index.html
- Анимация «VIBECRAFT»: VIBE вылетает слева, CRAFT — справа (0.8s cubic-bezier)
- Подзаголовок «Ai Школа» плавно проявляется после соединения (fade-in с задержкой 0.9s)
- Две одинаковые карточки (1:1 по размеру): «АТТЕСТАЦИЯ 66AI» и «VIBECRAFT»
- Карточки со скруглёнными углами и эффектом «выдавленности» (многослойная тень card-extrude)
- 3D-параллакс на Vanilla JS: tilt до 4° при движении мыши, текст внутри стабилен (translateZ)
- Scroll-hint (bounce) внизу Gateway для подсказки прокрутки
- Обновлён docs/BRIEF.md

Изменённые файлы
- `index.html` — добавлена Gateway-секция, CSS-анимации, Vanilla JS для tilt-параллакса
- `docs/BRIEF.md` — обновлён статус задач

Верификация
- [x] Анимация соединения Vibe + Craft работает (vibe-enter / craft-enter keyframes)
- [x] Карточки идентичны по размеру (grid grid-cols-1 md:grid-cols-2, одинаковые padding)
- [x] 3D-параллакс мягкий (MAX_TILT=4°) и не затрагивает текст отдельно (tilt-text с translateZ)
- [x] На мобильных карточки вертикально (grid-cols-1), tilt не ломает layout
- [x] Нет jQuery — только Vanilla JS

Побочные эффекты / риски
- Parallax работает только на десктопе (mousemove). На мобильных карточки статичны — это корректно.

Открытые вопросы
- ref.jpg отсутствует в проекте — визуальная проверка выполняется без референса.
- Ссылка для правой карточки ведёт на `#program` (якорь внутри index.html) — может потребоваться уточнение.  