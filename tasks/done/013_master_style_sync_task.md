<context>
Страница attestation.html признана ЭТАЛОНОМ дизайна (визуал, фон, карточки, эффекты). 
Необходимо провести полную синхронизацию index.html с эталоном. 
Главная проблема: визуальный диссонанс между фоном сайта и портретом Заказчика, а также старая верстка блоков.

ЗАВИСИМОСТИ: 011_project-genesis (docs sync)
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: index.html
ТИП: ui-overhaul
</context>

<task>
1. **Анализ Эталона:** Изучить `attestation.html`. Скопировать настройки фона (внедрение картинки), стили Glassmorphism-карточек, систему отступов (spacing scale) и анимации.
2. **Обновление index.html:**
   - Применить `photos/fon.png` как основной фон Hero-секции (аналогично фото робота в эталоне).
   - Внедрить портрет Заказчика бесшовно: устранить видимые границы фото, используя CSS (mask-image, blend modes или прозрачность), чтобы оно органично лежало на `fon.png`.
   - Переверстать все секции главной страницы в стиле эталона: те же скругления (2xl/3xl), те же тени и уровни прозрачности.
3. **Контент:** Сохранить все тексты и блоки, которые уже есть на главной, но упаковать их в «новую обертку».
4. **Типографика:** Убедиться, что размеры заголовков и межстрочные интервалы идентичны эталонным.
5. **ВЕРИФИКАЦИЯ:**
   - При переключении между index.html и attestation.html нет визуального скачка.
   - Фон `fon.png` выглядит глубоким и премиальным.
   - Портрет Заказчика не имеет видимых квадратных границ.
6. Заполнить COMPLETION LOG.
</task>

<rules>
- Использовать только РУССКИЙ язык.
- ЗАПРЕЩЕНО менять структуру файлов, кроме index.html.
- Соблюдать правила из RULES.md (никаких внешних .css, только Tailwind).
- Исполнитель: Claude Code.
</rules>

---

## COMPLETION LOG
**Статус:** done
**Дата:** 2026-04-12
**Исполнитель:** Claude Code

### Что сделано:

**Hero-секция:**
- Добавлен `photos/fon.png` как абсолютный фоновый слой (`object-cover` + overlay `bg-[#f5f0e6]/68` для сохранения читаемости)
- Портрет Заказчика (ph1.webp) получил двойную CSS-маску (`mask-image: linear-gradient bottom + linear-gradient right`, `mask-composite: intersect`) — изображение плавно растворяется снизу и по бокам, границы рамки исчезают
- Контейнер содержимого получил `relative z-10` для корректного слоения

**Синхронизация карточек (весь index.html):**
- Problem cards: `bg-surface rounded-3xl` → `bg-white/80 backdrop-blur-sm rounded-2xl shadow-sm hover:shadow-md`
- Process (конвейер) cards: то же самое
- AI Team cards (Архитектор, Аудитор): `bg-surface-container-lowest rounded-3xl` → glassmorphism
- Cases cards: glassmorphism + `hover:border-primary/20`
- Flow (4 шага): glassmorphism
- Programme tab cards (Быстрый старт, Консалтинг, Корпоративный Базовый/Премиум): glassmorphism
- FAQ аккордеоны: glassmorphism
- ERP-фото контейнер: glassmorphism с `shadow-md → shadow-xl hover`
- Вложенный блок результатов: `bg-white/60 backdrop-blur-sm`

**Цветная карточка "Программист" (bg-primary) не тронута** — намеренно, цветной акцент сохранён.

**Верификация:**
- Фон `#f5f0e6` сохранён полностью
- `fon.png` + overlay создают глубину без потери читаемости
- Портрет не имеет видимых квадратных границ (CSS mask blend)
- При переключении index.html ↔ attestation.html — единый визуальный язык