<context>
Мы наращиваем бизнес-логику для секции программы (#program), интерфейс которой был реализован в задаче 001. Нам нужно добавить интерактивный калькулятор выгоды, глубокую маршрутизацию (URL-хеши для табов) и управление отображением цен на основе URL-параметров (UTM).
</context>

<task>
1. [Калькулятор Флагмана] Внутри таба «Бизнес-инженер» добавить логику подсчета:
   - Сделать модули 2, 3 и 4 (каждый по 14 990 ₽) выбираемыми (чекбоксы или активные карточки).
   - Внизу блока выводить динамическую панель: «Сумма выбранных модулей: X ₽».
   - Рядом выводить сравнение с покупкой всего курса: «Полная программа: 36 000 ₽ (Ваша экономия: Y ₽)».
2. [URL-маршрутизация табов] Настроить чтение хеша из URL при загрузке страницы:
   - Если URL содержит `#program-start` -> активировать таб «Быстрый старт».
   - `#program-engineer` -> таб «Бизнес-инженер».
   - `#program-business` -> таб «Для бизнеса».
   - При клике на табы хеш в адресной строке должен обновляться без перезагрузки страницы (History API).
3. [Управление ценами / UTM-логика] Написать функцию-перехватчик:
   - Если в URL присутствует параметр `?utm_custom_price=hide` (или аналогичный тестовый триггер), скрывать все элементы с ценами на странице и заменять их на текст «Специальные условия (уточняйте у менеджера)».
4. [Файловая структура] ПОСЛЕ успешного выполнения всех пунктов перемести этот файл (`002_TASK_program_logic.md`) из папки `todo` в папку `done`.
</task>

<rules>
- Логика калькулятора должна строго опираться на цены из `WEBSITE_CONTENT.md`.
- Использовать чистый JavaScript (или стейт-менеджер текущего фреймворка), код должен быть модульным и легко расширяемым.
- Переключение хешей в URL не должно вызывать дерганый скролл страницы, всё должно работать плавно.
- Не ломать верстку и 3D-анимации, добавленные в предыдущем таске.
</rules>