📋 Прочитай docs/BOOT.md перед выполнением этого задания.

<context>
Жесткая компановка основных страниц (index.html, price.html, otchet.html, attestation.html).
Требование заказчика: НИКАКОГО СКРОЛЛА. Весь контент должен помещаться в один экран монитора (100vh). Дизайн всех страниц должен быть приведен к единому стилю (как у price.html).

ЗАВИСИМОСТИ: 030_gateway-typography-layout_task, 033_header-logo-refinement_task
ЗАТРАГИВАЕМЫЕ ФАЙЛЫ: index.html, price.html, otchet.html, attestation.html
ТИП: ui | refactor
</context>

<task>
1. Применить правило "Одного экрана" к `index.html` (Gateway):
   - Обернуть `<body>` или `<main>` в классы `h-screen overflow-hidden flex flex-col justify-center`.
   - Поднять H1 "VibeCraft" максимально высоко (убрать лишние `mt-`, `pt-`).
   - Уменьшить отступы между заголовком, подзаголовком, карточками и слоганом, чтобы они гарантированно влезали в экран.
2. Применить правило "Одного экрана" к `price.html`:
   - Установить `h-screen overflow-hidden flex flex-col`.
   - Заголовок H1 сместить вплотную к хедеру.
   - Скомпоновать таблицы/калькулятор (уменьшить padding'и, сделать шрифт чуть плотнее: `text-sm`, `py-2` вместо `py-4`), чтобы всё уместилось.
3. Применить правило "Одного экрана" к `otchet.html` и `attestation.html`.
4. Дизайн: перенести визуальный стиль панелей/карточек из `price.html` на остальные страницы раздела Аттестации, чтобы они выглядели единой системой.
5. ВЕРИФИКАЦИЯ:
   - Открыть каждую из 4 страниц на десктопном разрешении (1080p).
   - Убедиться, что вертикальный скроллбар браузера ОТСУТСТВУЕТ (`overflow-hidden`).
   - Ни один элемент не "обрезается" снизу.
6.  Страница Аттестация: убрать кнопку "Рассчитать стоимость", Кнопку запустить Аттестацию - поставить по центру.
7.  Страница Аттестация: для кнопки Запустить Аттестацию - разработать свою модалку для заполнения-  поля с колличеством сотрудников названием компании контактов.
8. Заполнить COMPLETION LOG и перенести в done/.
</task>

<rules>
- ИСПОЛНИТЕЛЬ: Claude Code (критическая UI-задача).
- ВАЖНО: Если контент физически не влезает на маленьких экранах (мобилки), разрешается включать внутренний скролл только для самого контентного блока (`overflow-y-auto` на внутреннем `div`), но глобальный `body` всё равно не должен скроллиться на десктопе.
</rules>

---

## COMPLETION LOG
**Статус:** _pending_
**Дата завершения:** ___
**Исполнитель:** ___

### Сделано
- ___

### Изменённые файлы
- ___

### Верификация
- [ ] Скролл на десктопе отсутствует для index, price, otchet, attestation
- [ ] H1 "VibeCraft" поднят на главной странице
- [ ] Калькулятор на price.html ужался по высоте